#ifndef TELA_H
#define TELA_H

class Tela {
public:
    // Método estático para limpar a tela
    static void limpar();

    // Método estático para configurar o idioma e encoding
    static void configurarIdioma();
};

#endif 
